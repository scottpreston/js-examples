{
  "author": "Scott Preston",
  "name": "hello-es6-modules",
  "description": "a sample project for getting started with ES6 modules",
  "version": "0.0.1",
  "scripts": {
    "build": "babel es6 --out-dir src && browserify src/*.js -o app.js",
    "prestart": "npm run build",
    "start": "http-server '-p 5000'"
  },
  "dependencies": {
    "http-server": "^0.9.0"
  },
  "devDependencies": {
    "babel-cli": "^6.6.0",
    "babel-preset-es2015": "^6.6.0",
    "browserify": "^13.0.0"
  }
}
